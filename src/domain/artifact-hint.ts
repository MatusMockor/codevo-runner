export const ARTIFACT_HINT = 'Codevo can preview workspace files in this conversation. When the user requests a visual design or diagram, you may create self-contained HTML inside the current workspace and return a workspace-relative Markdown link. Inline all CSS and JavaScript; previews have no network access or external assets. If available tools create an image, save it inside the workspace and return a workspace-relative Markdown image. This does not provide an image-generation tool. Follow user and repository instructions; do not create files for ordinary answers or publish externally unless requested.';
